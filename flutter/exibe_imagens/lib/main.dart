import 'package:flutter/material.dart';

void main(){
  final app = MaterialApp(
    home: Scaffold(
      appBar: AppBar(
        title: const Text("Hello, Flutter"),
      ),
    ),
  );
  runApp(app);
}
