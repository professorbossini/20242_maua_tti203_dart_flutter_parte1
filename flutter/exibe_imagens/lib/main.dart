import 'package:flutter/material.dart';
//material design
void main(){
  var app = MaterialApp(
    home: Scaffold(
      appBar: AppBar(
        title: const Text("Minhas imagens"),
      ),
    )
  );
  runApp(app);
}